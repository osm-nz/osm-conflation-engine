{
  "name": "@osm-conflation-engine/server",
  "private": true,
  "type": "module",
  "types": "src/index.types.ts",
  "main": "src/index.types.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/osm-nz/osm-conflation-engine.git",
    "directory": "packages/server"
  },
  "scripts": {
    "postinstall": "wrangler types && drizzle-kit generate && rm -rf .wrangler && npx wrangler d1 execute d1_db --local --file=$(ls ./migrations/*.sql| head -1)",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc",
    "test": "vitest"
  },
  "dependencies": {
    "chanfana": "^3.3.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.12",
    "jose": "^6.2.3",
    "osm-api": "^4.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.107.0"
  }
}
